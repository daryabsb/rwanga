# S2.2 — Installed-app smoke (packaged build)

- **Date:** 2026-07-26 · **HEAD at test:** `b323820f`
- **Artifact:** `rwanga-editor\build\output\Rwanga Editor-Setup-0.1.0-alpha.0.exe` (76 MB, NSIS x64,
  oneClick, per-user) — built in S2.1 under Windows Developer Mode.
- **Signing decision (plan Decision #2):** user accepted the UNSIGNED installer for LR-01 TRUE on
  2026-07-26 ("yes"); signing remains deferred per GO_LIVE "(+ signing later)" / SignPath prep plan.
- **Install method:** silent (`/S`) → `%LOCALAPPDATA%\Programs\rwanga-editor\Rwanga Editor.exe`.
- **Smoke performed by:** the user, by hand, on the INSTALLED app (not `npm start`).

## Verdicts

| Item | Check | Verdict | Notes |
|---|---|---|---|
| a | App opens to a usable editor, no error dialog | **PASS** | Opened restoring the dev session's last file (`playground-the-last-light.rga`) — packaged app shares userData with dev app → recorded as **GAP-2-2** (observation, not a smoke failure) |
| b | File → New; type scene heading + action + dialogue | **PASS** | Typing, slug recognition (`INT. THE GARDEN — DAY`), block structure, scene navigator, word count all live. Long-standing visual defect surfaced and ticketed: New doc renders a dead band above a shrunken page that grows as content arrives → **GAP-2-1** (user-ratified expected behavior: full configured page size from first paint) |
| c | Save to Documents as `smoke.rga` | **PASS** | |
| d | Close app fully, relaunch, reopen `smoke.rga` — content intact | **PASS** | |
| e | Print Preview opens and renders | **PASS** | User: "needs improvement later, but pass as feature" — polish noted, no new gap row (print-polish backlog exists) |

**Result: 5/5 PASS.** Two gap rows opened (GAP-2-1 visual defect, GAP-2-2 dev/prod userData sharing) —
neither blocks LR-01, which asserts a working installer + installed-app core flow.
