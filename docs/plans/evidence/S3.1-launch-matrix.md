# S3.1 — PF-01 cold-start launch matrix (Windows, installed app)

- **Date:** 2026-07-26 · **HEAD at test:** `54f8eab2` · **App:** installed package
  `%LOCALAPPDATA%\Programs\rwanga-editor\Rwanga Editor.exe` (from `Rwanga Editor-Setup-0.1.0-alpha.0.exe`, S2.1/S2.2)
- **Decision #1 (macOS scope), user 2026-07-26:** macOS stays IN scope; user will provide a Mac when
  needed. Windows matrix below; the macOS matrix (+ `pack:mac` + smoke) is a deferred PF-01/LR-01
  sub-task executed when the Mac arrives. PF-01 stays PARTIAL until reboot item + macOS complete.

## Matrix results

| # | Case | Result | Notes |
|---|---|---|---|
| 1–10 | 10 consecutive cold starts (full close between) | **10/10 PASS** | Scripted `Start-Process` + poll for main window; times: 1.6, 1.3, 1.3, 1.2, 1.2, 1.2, 1.3, 1.2, 1.1, 1.1 s to window. User observing; no error dialogs reported |
| 11 | Launch while another instance is running | **PASS** | Single-instance lock: process count unchanged (4 Electron procs = one app), one window, existing instance focused. Plan accepts either behavior; recorded: **focus-existing**. No data corruption observed |
| 12 | `.rga` double-clicked from Explorer | **OBSERVED: no association** | Installer registers no `.rga` file association (`assoc .rga` → not found; no Explorer UserChoice). Per plan: observed behavior, NOT a failure. Candidate improvement: `fileAssociations` in electron-builder config |
| 13 | Launch immediately after reboot | **PENDING — user action** | Requires machine reboot (ends the agent session). To complete: reboot, launch the installed app once from Start menu, confirm it reaches a usable editor with no error dialog |

**Interim verdict: 12/13 complete, 0 failures.** PF-01 stays PARTIAL until item 13 (reboot) is
recorded and the macOS matrix runs (Decision #1). No gap rows opened by this slice so far.
